{
  "name": "learning-english-monorepo",
  "private": true,
  "workspaces": [
    "apps/web",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev -w apps/web",
    "build:web": "npm run build -w apps/web"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
